spring:
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false

  datasource:
    db2:
      url: jdbc:postgresql://localhost:8822/test_db
      username: postgres
      password: 123454321
      driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8090
  servlet:
    context-path: /api/v1

logging:
  level:
    com.asmetsalud.nexus: DEBUG
    org.hibernate.SQL: DEBUG